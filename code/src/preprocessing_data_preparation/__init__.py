"""Data preprocessing & transformation.

QC + split generation (qc_and_split.py),
the crop/resample/normalize/augment pipeline (preprocessing.py), and the
FeTADataset class other roles build against (dataset.py).
"""
